# ネームスペース

EMQX 5.9.0 以降、ネームスペース機能により、ユーザーは MQTT クライアントを論理的にグループ化し、単一の EMQX クラスター内でトラフィック制限を適用できるようになりました。この機能は、複数のクライアントグループ（事業部門、アプリケーション、顧客など）が同じインフラを共有しつつ、論理的に分離されたスケーラブルなデプロイメントを可能にします。

EMQX のネームスペース機能は以下の2つの部分で構成されています：

- **MQTT クライアントネームスペース** — MQTT クライアントを（ユーザー名、SNI、その他の接続メタデータによって）論理的にグループ化し、ネームスペースごとのクォータ、レート制限、クライアントIDおよびトピックの分離設定を提供します。
- **管理ユーザーネームスペース** — ダッシュボード、CLI、API ユーザーを [namespaced roles](../dashboard/system.md#namespaced-roles) により特定のネームスペースにスコープし、委譲された管理者が割り当てられたネームスペース内のリソースのみを閲覧・操作できるようにします。EMQX 6.0 以降で利用可能です。

::: warning 信頼されたデプロイメントのみ

**管理ユーザーネームスペース** は、同一組織内のチームや事業部門を分離するなど、信頼された内部デプロイメント向けに設計されており、誤って相互の設定を変更するリスクを軽減します。これは強力な分離保証を提供せず、パブリックまたは信頼されていないマルチテナント環境のセキュリティ境界ではありません。パブリックマルチテナント環境で管理ユーザーネームスペースの利用を検討されている場合は、EMQ の営業までお問い合わせください。このユースケースは現時点で標準機能としてはサポートされていません。

**MQTT クライアントネームスペース** に関しては、ネームスペース間のクライアント分離はオプトインであり、明示的な設定が必要です。ネームスペース間のクライアントが相互に信頼されていない場合は、[分離メカニズム](#isolation-mechanisms) を参照し、クライアントIDのオーバーライドとトピックのマウントポイントを適切に設定してください。

:::

## ネームスペースとは

EMQX Enterprise のネームスペースは、MQTT クライアントの論理的分離とリソース管理のための仕組みです。異なる事業やテナントのクライアントを共有クラスター内で別々のネームスペースに分割し、接続、メッセージ、クォータなどの分離を実現します。

ネームスペースは、クライアントの接続メタデータ（ユーザー名や Server Name Indication（SNI）など）から抽出される特別なクライアント属性 `tns`（テナントネームスペース）で識別されます。

> **典型的なユースケース**：企業内の複数事業部門がクラスターを共有、テナントレベルのリソース分離管理、集中アクセス制御など。

### ネームスペースで実現できること

- **クライアントとメッセージの論理的分離**

  ネームスペースにより、異なるテナントのクライアントを論理的に分離し、クライアントIDやトピックスペースを分離できます。

  ::: tip 補足

  ネームスペースを有効にしただけでは、クライアントIDのオーバーライドやトピックプレフィックスは自動的に適用されません。これらは手動で設定する必要があります。詳細は [分離メカニズム](#isolation-mechanisms) をご参照ください。

  :::

- **テナント単位のクォータおよび接続制御**

  ネームスペースごとに同時接続数やメッセージパブリッシュレートの制限を設定でき、公平な利用とシステムの安定性を確保します。

- **強化されたログと運用可視性**

  ログには自動的にネームスペース識別子（`tns`）が含まれ、クライアントの活動追跡や問題検出、テナント単位の診断が容易になります。

- **ネームスペースベースのリソース監視**

  ネームスペースはテナントごとの接続数やメッセージスループットなどのメトリクス収集の境界を提供し、容量計画や運用インサイトに不可欠です。

- **管理ユーザーの分離**

  EMQX 6.0 以降、ネームスペースはダッシュボード、CLI、API ユーザーにも [namespaced roles](../dashboard/system.md#namespaced-roles) を通じて拡張されています。信頼モデルや必要な安全策については本ページ冒頭の [信頼されたデプロイメントのみ](#namespace) の注意事項をご覧ください。

  - 管理ユーザーは特定のネームスペースに限定したロール（例：`ns:team_a::administrator`）で作成可能です。
  - ネームスペースユーザーは割り当てられたネームスペース内のリソースのみを閲覧・操作できます。
  - ネームスペース非対応のクラスター全体設定は閲覧のみ可能で、変更はグローバル管理者のみが行えます。
  - EMQX 6.0.4 以降、ネームスペース管理者は [自身のネームスペース内で API キーを管理可能](../api.md#manage-api-keys-as-a-namespaced-administrator) です。グローバルキーや他ネームスペースのキーはアクセス不可です。
  - これにより、データ分離とともにテナント固有の安全な管理アクセスが実現します。

- **マルチテナント管理**

  システム管理者は同一クラスター内で複数のネームスペースを管理でき、各テナントは分離されたリソースとユーザー権限の自己完結型環境で運用可能です。

## 管理者ネームスペースの運用セキュリティ

委譲されたネームスペース管理者は、コネクター、ブリッジ、アクションなどのアウトバウンドターゲットを設定できます。追加の制御がなければ、意図しない内部または機密ネットワークへのアクセスを許してしまう可能性があります。

`rule_engine.ssrf` を有効にすると、コネクター設定のテスト、作成、更新時に HTTP および MQTT コネクターのターゲットを検証します。EMQX 6.0.4 以降、このポリシーは他のコネクタータイプやランタイム接続には適用されません。コネクタータイプを問わずアウトバウンドネットワーク境界を強制するには、EMQX ホスト側で以下のイグレス制御を行ってください：

- IdP、Webhook、コネクターバックエンドなど承認済み宛先へのアウトバウンドアクセスのみ許可する。
- インスタンスメタデータサービス、ループバックアドレス、リンクローカルアドレス、内部管理ネットワークへのアクセスは明示的に必要な場合を除き拒否する。典型的なブロック対象エンドポイントは `100.100.100.200`、`169.254.169.253`、`169.254.169.254`、`fd00:ec2::254` など。
- 新しい統合や管理機能でアウトバウンド HTTP/TCP 接続を開始する場合は、ファイアウォールルールを必ず見直してください。

詳細は [ルールエンジンポリシーとファイアウォールルールによる SSRF 緩和](../cluster/security.md#mitigate-ssrf-with-rule-engine-policy-and-firewall-rules) をご覧ください。

## 分離メカニズム

EMQX は高い柔軟性を持ち、ネームスペース機能導入前から様々な分離方法をサポートしてきました。ネームスペース機能は統一されたテナント識別子フィールド（`client_attrs.tns`）を提供し、クライアントIDやトピックのマウントポイントなどの設定をテナント情報に基づいて整理・管理できます。

クライアントIDやトピックの分離は自動的には有効になりません。信頼モデルやビジネス要件に応じて手動で設定してください。EMQX 6.0.3 以降、ルールトリガーの分離はデフォルトで有効ですが、これはネームスペース内のルールをトリガーするメッセージやイベントを制限するだけで、クライアントIDやトピックの分離に代わるものではありません。

- **クライアントIDオーバーライド**

  ::: warning 信頼されていないマルチテナント環境で必須

  異なるネームスペースのクライアントが相互に信頼されていない場合（例：各ネームスペースが外部顧客や別組織を表す場合）、`mqtt.clientid_override` の設定が必須です。これを設定しないと、あるネームスペースのクライアントが別テナントのクライアントIDを再利用し、そのクライアントをオフラインにしたり、永続セッションを乗っ取ったり、サービス拒否を引き起こす可能性があります。認証はこれを防げません。セッション乗っ取りは ACL 適用前の接続レイヤーで発生します。

  これに加え、[マウントポイントを使ったトピック分離](#isolation-mechanisms) を組み合わせて、トピックレベルでのネームスペース越えアクセスも防止してください。

  :::

  異なるネームスペースのクライアントが同じクライアントIDを使って EMQX に接続したい場合は、クライアントIDオーバーライドルールを設定できます。例えば：

  ```hocon
  mqtt.clientid_override = "concat([client_attrs.tns, '-', clientid])"
  ```

  このルールはクライアントIDの前にネームスペースをプレフィックスとして付加し、競合を回避します。

- **マウントポイントを使ったトピック分離**

  異なるネームスペースのクライアントが同じトピック名をパブリッシュ／サブスクライブしても互いに影響しないように、マウントポイントを使ってネームスペースのプレフィックスを自動付加できます：

  ```
  listener.{TYPE}.{NAME}.mountpoint = "${client_attrs.tns}/"
  ```

  この設定により、トピック名にネームスペースプレフィックスが付加されます。

- **ルールトリガーの分離**

  `rule_engine.limit_selects_in_namespace` を有効にすると、ネームスペースに属するルールは同じネームスペースのクライアントからのメッセージやクライアント関連イベントによってのみトリガーされます。EMQX はクライアント属性 `client_attrs.tns` によってクライアントのネームスペースを識別します。

  この設定は、他のネームスペースからのメッセージやイベントによるルールトリガーを防止するだけで、クライアントのネームスペース間のパブリッシュやサブスクライブを制限するものではありません。クライアントのトピックアクセスを分離するには、マウントポイントを使ったトピック分離と認可ルールの設定が必要です。

ネームスペース分離の現状と今後の計画については、[マルチテナンシーロードマップ](#multi-tenancy-roadmap) をご覧ください。

## ネームスペースの有効化

ネームスペース機能を有効にするには、まず EMQX にクライアントがどのネームスペースに属するかを判定させる必要があります。これは `tns`（テナントネームスペース）という特別なクライアント属性を設定することで行います。

### 設定ファイルによるネームスペース有効化

`tns` 属性は、クライアントの接続メタデータ（ユーザー名、SNI、その他のフィールド）から抽出できます。

例えば、クライアントのユーザー名をネームスペース識別子として使う場合、以下の設定を適用します：

```
mqtt.client_attrs_init = [{expression = username, set_as_attr = tns}]
```

### ダッシュボードによるネームスペース有効化

EMQX ダッシュボードからもネームスペースを有効にできます：

1. **管理** -> **MQTT 設定** -> **一般** タブの **クライアント属性** セクションに移動します。
2. **追加** をクリックし、以下を入力します：
   - **属性**：`tns`
   - **属性式**：例えばクライアントのユーザー名をネームスペース識別子にする場合は `username` と入力します。他の変数も使用可能です。属性式の詳細は [クライアント属性の設定](../../develop/client-attributes/client-attributes.md#set-client-attributes) を参照してください。
3. **変更を保存** をクリックします。

## マルチテナンシーロードマップ

以下の機能が段階的に展開されています：

- 管理ネームスペースと MQTT ネームスペースの統合
- 組み込みデータベース認証の分離実装
- 組み込みデータベース認可の分離実装
- 保持メッセージのクォータ分離実装
- Prometheus メトリクスの分離実装

::: tip 更新情報

EMQX 6.0 以降、ルール、アクション、ソース、コネクターの分離は完全に実装されており、ロードマップの対象外となっています。
:::

## 次のステップ

ネームスペースの概要と実現可能なことを理解したら、EMQX での利用を開始するために以下を参照してください：

- **[ネームスペースの作成](./create-namespace.md)**  
  ダッシュボードや REST API でネームスペースを明示的に作成する方法や、クライアントメタデータに基づく自動作成方法を学べます。
- **[ネームスペースの設定と管理](./configure-manage-namespace.md)**  
  ダッシュボードや REST API を使ったレート制限やセッションクォータの設定方法を解説します。
- **[クイックスタート：ネームスペースを体験する](./namespace-quick-start.md)**  
  MQTTX を使ったハンズオンガイドで、ネームスペースベースのクライアント・トピック分離を素早く試せます。
