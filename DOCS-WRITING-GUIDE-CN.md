# EMQX 中文文档编写指南

本指南用于规范本仓库中 EMQX 中文文档的结构、写作、格式和评审方式。它不只是语言风格指南，还覆盖信息架构、技术写作、Markdown 和 VitePress 用法、EMQX 术语、中文规范和评审标准。

创建新页面、更新已有页面、评审 PR 或将技术内容翻译为中文时，请参考本指南。

## 核心原则

所有 EMQX 中文文档都应遵循以下原则：

- **准确**：保留技术含义、产品行为、配置路径、API 路径、参数名称、示例、限制和版本信息。
- **清晰**：尽早说明重点，使用具体术语，避免模糊表达。
- **简洁**：使用短句和直接表达，删除空泛背景和冗余修饰。
- **完整**：提供读者完成任务所需的前提条件、操作步骤、示例、预期结果和限制。
- **一致**：在同一页面和相关页面中保持术语、格式、大小写、UI 文案和表达方式一致。
- **面向用户**：说明用户可以通过 EMQX 完成什么，而不只是描述 EMQX 的内部实现。
- **便于 AI 消费**：使用语义清晰的标题、自包含示例和结构化内容，便于搜索、检索增强生成和 AI 工具处理。

除非页面明确面向初学者，否则默认读者为技术用户。

## 信息类型

请根据页面或章节目的组织内容。一个页面可以包含多种信息类型，但每个章节应保持主题集中。

### 概念型内容

概念型内容用于说明某个对象是什么、为什么重要，以及何时使用。

概念型内容应：

- 以简短定义或摘要开头。
- 说明问题背景、使用场景，或与其他 EMQX 功能的关系。
- 避免包含完整操作流程，除非短示例有助于理解。
- 链接到任务型或参考型页面，提供配置和操作细节。

### 任务型内容

任务型内容用于帮助用户完成一个明确目标。

任务型内容应包含：

- 目标导向的标题，例如“创建连接器”或“为 MQTT 监听器配置 TLS”。
- 必要的前提条件。
- 使用祈使句编写的编号步骤。
- 有助于完成任务的配置示例或截图。
- 可验证结果时，提供验证步骤。
- 指向高级选项或参考信息的相关链接。

### 参考型内容

参考型内容用于查询配置字段、API 参数、CLI 选项、限制、默认值或兼容性说明。

参考型内容应：

- 使用表格或定义列表组织结构化信息。
- 保持描述准确、并列结构一致。
- 在相关位置提供单位、默认值、允许值和版本范围。
- 避免在参数说明中插入过长的概念解释。

### 故障排查内容

故障排查内容用于帮助用户诊断并解决问题。

故障排查内容应包含：

- 现象或错误信息。
- 可能原因。
- 诊断步骤。
- 解决方法。
- 验证问题已解决的方法。

### 发布说明

发布说明应尽量贴近源变更。

编辑发布说明时：

- 保留 PR 编号、Issue 链接、功能名称、API 路径、配置路径和版本号。
- 只有行为完全一致时，才可复用其他版本的表述。
- 优先说明用户可感知的行为变化。
- 避免营销化表达。
- 对已完成的变更使用过去时或中文中对应的已完成表达。

## 页面结构

### 标题

每个 Markdown 页面必须有且只有一个一级标题。一级标题必须清楚描述页面主题。

标题层级必须按顺序使用：

```markdown
# 配置 MQTT 监听器

## 添加 TCP 监听器

### 配置监听器选项
```

不要跳过标题层级。不要只为视觉强调而使用标题。

中文标题使用自然的中文标题样式，不使用英文 Title Case 规则。标题应简短、具体，并尽量使用名词短语或动宾结构。

推荐：

```markdown
## 创建连接器
## 配置 TLS 证书
## 查看规则执行结果
```

不推荐：

```markdown
## 关于连接器的一些说明
## 如何去进行 TLS 证书的配置
```

不要在无关修改中调整已有标题风格。同一页面内的标题风格应保持一致。

### 引言

每个页面开头应使用简短引言回答：

- 本页面介绍什么？
- 读者阅读后可以完成什么？
- 页面适用于哪个产品、版本、版本形态或部署方式？

避免使用空泛表述，例如“本文主要介绍……”。如果可以直接说明页面价值，应优先使用直接表达。

### 前提条件

当用户必须先完成某些准备工作才能执行任务时，使用“前提条件”章节或 `tip` 容器。

只列出当前任务必需的前提条件。不要把“前提条件”写成长篇背景说明。

### 操作步骤

多步骤操作使用编号列表。

操作步骤规则：

- 使用祈使句，例如“点击”“选择”“输入”“配置”“运行”。
- 每个步骤使用完整句子，并以句号结尾。
- 每个步骤聚焦一个用户操作，或一组发生在同一位置的相关操作。
- 可选步骤以“[可选]”开头。
- 不要遗漏最后一步，例如点击**创建**、**保存**或**应用**。
- 可以验证结果时，添加验证步骤。

示例：

```markdown
1. 在左侧导航栏中，点击**数据集成** -> **连接器**。
2. 点击**创建**。
3. 在连接器类型列表中选择 **MQTT**。
4. 配置必填字段。
5. 点击**创建**。
6. 确认连接器状态为**已连接**。
```

尽量保持操作流程简短。如果步骤过长，应拆分为多个阶段或独立任务章节。

### 相关链接

当用户需要参考信息或后续任务时，添加相关链接。

不要因为目标页面提到了相同功能就随意添加链接。链接文字必须说明目标页面内容。

## 中文语言风格

### 语气、人称和时态

使用第二人称“你”指代读者。描述系统行为时，使用 `EMQX` 或完整产品名称。

优先使用主动语态和直接表达。

仅在以下情况使用被动表达：

- 执行动作的主体未知或不重要。
- 需要强调动作的承受对象。
- 主动表达会在错误、警告或通知中显得像是在责备用户。

示例：

| 推荐 | 不推荐 |
| --- | --- |
| EMQX 将配置保存到 `cluster.hocon`。 | 配置被 EMQX 保存到 `cluster.hocon`。 |
| 文件已保存。 | 你保存了文件。 |
| 检测到 50 多个冲突。 | 你创建了 50 多个冲突。 |

### 用词和句子

使用简单、准确、专业的中文表达。

规则：

- 使用短句，避免过长的复合句。
- 避免“进行”“实现了对……的支持”“通过……来……”等冗余表达。
- 避免“非常”“十分”“很容易”“显然”“只需”等不必要副词。
- 避免一个句子中连续使用多个“和”“或”“但”连接分句。
- 避免在段落内部强制换行。
- 避免过度碎片化段落，但为提高可读性可以使用较短段落。

推荐：

```markdown
配置完成后，EMQX 会立即应用该规则。
```

不推荐：

```markdown
在配置完成以后，EMQX 会通过内部机制来实现对该规则的立即应用。
```

### 包容性语言

避免使用可能引起不必要偏见或具有历史负面含义的词语。

| 推荐 | 不推荐 |
| --- | --- |
| 主/从、主节点/从节点，或根据语境使用主备、上游/下游 | master/slave |
| 边界网络 | 非军事区、DMZ |
| 停止响应 | 挂起、挂死 |

如果英文 UI、协议或第三方产品中保留了原始术语，应按实际名称书写，不自行改名。

### 避免时间依赖表达

技术文档可能在很久以后仍被阅读。避免使用依赖写作时间的词语。

避免：

- 目前
- 当前
- 现在
- 最新
- 新版
- 旧版
- 未来
- 即将
- 暂不支持
- 后续支持

如果功能与版本相关，应明确说明版本。

推荐：

```markdown
从 EMQX 5.8.4 开始，EMQX 提供 `base.hocon` 文件。
```

不推荐：

```markdown
最新版本现在提供新的 `base.hocon` 文件。
```

## 中文标点、数字和单位

中文文档应遵循中国大陆现行中文书面语规范。遇到未覆盖的情况，优先保持本仓库已有中文文档的一致性。

### 标点符号

中文正文使用中文全角标点，代码、命令、配置项、API 路径、URL 和英文原文除外。

规则：

- 陈述句末尾使用句号“。”。
- 并列词语之间使用顿号“、”，但英文术语并列时可根据可读性使用逗号。
- 引用中文内容或强调特殊含义时，使用中文双引号“”。
- 引号内部再引用时，使用中文单引号‘’。
- 书名、标准名、文档名可使用书名号《》。
- 冒号用于引出说明、列表或示例。
- 避免使用英文破折号。中文解释说明如确有需要，可使用中文破折号“——”，但技术文档中优先改写为短句。

示例：

```markdown
配置完成后，点击**保存**。
有关详细信息，参见《标点符号用法》。
```

### 数字

技术文档中优先使用阿拉伯数字，特别是计量、编号、版本、端口、状态码、路径和配置值。

使用阿拉伯数字：

- 版本号：`EMQX 5.8.4`
- 端口号：`1883`
- 百分比：`80%`
- 配置值：`1024`
- HTTP 状态码：`404`
- 编号、地址、日期和时间

以下情况可使用汉字数字：

- 固定词语或约定俗成表达，例如“一致性”“二进制”。
- 概数，例如“三四个节点”“几十条规则”。
- 中文句子中不表示精确计量的一般叙述。

不要以阿拉伯数字开头写句子，应调整语序。

### 单位

量值和单位之间使用空格，除非单位或符号本身要求不加空格。

推荐：

```markdown
10 MB
5 秒
80%
```

规则：

- 保留技术来源中的单位和值。
- 同一页面中同类单位写法保持一致。
- 不随意把 `MB`、`MiB`、`ms`、`s` 等单位互换。
- 货币符号和数字之间不加空格，例如 `$0.01`、`¥0.01`。

## 术语和产品名称

正式产品名和功能名必须保持一致。

产品名称包括：

- EMQX
- EMQX Enterprise
- EMQX Open Source
- EMQX Cloud
- EMQX ECP
- HStreamDB
- Neuron
- NanoMQ
- XMeter
- MQTTX

不要为产品名或功能名自行创造缩写。

当版本形态会影响功能范围时，使用完整产品名。例如，企业版专属功能应写为 `EMQX Enterprise`。

首次出现时可写为“EMQX Dashboard”，后续可根据语境写作 `Dashboard`。如果页面需要与其他产品控制台区分，应保留完整名称。

## 英文术语和缩略语

中文文档中可以保留必要英文术语，但已有稳定中文译名或中文 UI 标签的术语应使用中文。以下是常见写法示例，不是完整术语表。具体术语应以当前产品 UI、现有中文文档和项目术语表为准。

常见写法示例：

| 英文术语 | 中文文档写法 |
| --- | --- |
| Connector | 连接器 |
| Rule Engine | 规则引擎 |
| Dashboard | Dashboard |
| HOCON | HOCON |
| API | API |
| CLI | CLI |

规则：

- 不翻译产品名、配置项、API 路径、CLI 命令、代码标识符和 UI 原文。
- 如果术语没有稳定中文译名，或中文 UI 中保留英文，则中文文档中也保留英文。
- 通用缩略语可直接使用，例如 `API`、`CLI`、`HTML`、`HTTP`、`JSON`、`MQTT`、`REST`、`SQL`、`TLS`、`URL`。
- 不为只出现一次的英文术语额外定义缩写。
- 不将缩写用作中文动词。
- 必要时首次出现可采用“中文解释（英文术语）”形式，例如“物联网（Internet of Things，IoT）”。

中文与英文、数字混排时，应按可读性处理空格。本仓库建议：

- 中文与英文产品名、协议名、配置文件名之间使用空格。
- 中文与行内代码之间使用空格。
- 中文标点前后不额外添加空格。

示例：

```markdown
EMQX 使用 HOCON 作为配置文件格式。
设置 `node.name` 后，重启 EMQX 节点。
```

## 格式

### 文本格式

格式使用应保持一致。

| 项目 | 格式 | 示例 |
| --- | --- | --- |
| UI 标签、按钮、字段、页签和菜单项 | 加粗 | 点击**创建**。 |
| 参数、变量、值、文件名、路径、目录、命令和代码 | 行内代码 | 在 `etc/emqx.conf` 中设置 `node.name`。 |
| 错误信息 | 行内代码或引号，按语境选择 | 如果看到 `connection refused`，检查监听器端口。 |
| 强调 | 谨慎使用加粗 | 此设置**不会**在节点间同步。 |

不要为了普通强调滥用加粗。不要对产品名或普通技术词使用行内代码格式。

### 表格

结构化参考信息应优先使用表格。

规则：

- 表格标题和表头使用简洁中文。
- 表格单元格使用自然中文表达。
- 同一列的内容类型和句式保持一致。
- 涉及配置项时，提供单位、默认值和允许值。
- 单元格为完整句子时使用句号；如果整列都是短语，可不加句号。

## UI 文案和交互

UI 标签必须与产品界面完全一致。

短导航路径使用 `->`，前后各保留一个空格，不加粗箭头。

示例：

```markdown
在 Dashboard 中，点击**数据集成** -> **连接器**。
```

常见 UI 元素写法：

| UI 元素 | 推荐写法 |
| --- | --- |
| 按钮 | 点击**创建**。 |
| 下拉列表 | 在**连接器类型**列表中选择 **MQTT**。 |
| 复选框 | 选中**启用 TLS**复选框。 |
| 文本框 | 在**名称**字段中输入 `emqx1`。 |
| 导航菜单 | 在左侧导航栏中，点击**监控**。 |
| 图标 | 在页面右上角，点击**设置**图标。 |
| 页面 | 在**创建连接器**页面中配置连接器。 |
| 页签 | 在**高级设置**页签中配置超时时间。 |
| 开关 | 打开**启用**开关。 |
| 对话框 | 在**删除连接器**对话框中，点击**确认**。 |
| 区域 | 在**连接信息**区域中配置服务器地址。 |
| 面板 | 在**已接收**面板中查看消息负载。 |

鼠标操作使用“点击”，键盘操作使用“按”。除非任务必须使用快捷键，否则避免使用键盘快捷键。

## EMQX 技术写作规则

### 配置文件和 HOCON

EMQX 配置文档必须符合目标版本的实际配置模型。

EMQX 5.x 及后续版本使用 HOCON 作为配置文件格式。

使用正确的配置文件名称和路径：

- `etc/base.hocon`：基础配置文件。从 EMQX 5.8.4 开始提供，用于保存可被更高优先级配置覆盖的默认设置。
- `data/configs/cluster.hocon`：集群级动态配置文件。Dashboard、REST API 和 CLI 产生的配置变更会持久化到此文件。
- `etc/emqx.conf`：用于关键系统设置的不可变配置文件，例如 `node` 和 `cluster`。
- 环境变量：以 `EMQX_` 为前缀的配置来源，优先级最高。

编写配置示例时：

- HOCON 示例使用 `hocon` 代码块。
- 使用真实配置路径，例如 `listeners.tcp.default.bind`。
- 明确示例适用于 `base.hocon`、`cluster.hocon`、`emqx.conf`、Dashboard、REST API 还是 CLI。
- 除非页面明确讲解恢复或高级维护，不要指导用户手动编辑 `cluster.hocon`。
- 配置文件或配置项与版本相关时，添加版本说明。
- 当字符串值包含 `#`、`:` 或 `=` 等特殊字符时，保留 HOCON 引号规则。

示例：

```hocon
listeners.tcp.default {
  bind = "0.0.0.0:1883"
  max_connections = 1024000
}
```

描述 EMQX 5.8.4 及后续版本的配置优先级时，使用以下顺序：

```text
base.hocon < cluster.hocon < emqx.conf < 环境变量
```

对于早于 EMQX 5.8.4 的版本，不要把 `base.hocon` 写成可用配置文件。

### CLI 命令

Shell 命令使用 `bash` 代码块。

规则：

- 提供可复制运行的命令。
- 说明必要的环境变量或工作目录前提。
- 除非需要区分输入和输出，否则不要添加 `$` 等命令提示符。
- 命令输入和命令输出分开展示。

### REST API

使用准确的 HTTP 方法和 API 路径。

示例：

- `GET /api/v5/clients`
- `POST /api/v5/rules`

API 路径、请求字段、响应字段和状态码使用行内代码格式。

### MQTT 示例

编写 MQTT 示例时，优先使用 MQTTX CLI 或 EMQX 原生工具。

规则：

- 使用真实合理的主题和载荷。
- 保留 MQTT 主题通配符语义。
- 区分主题名和主题过滤器。
- 当 QoS、保留消息标志、用户名、密码或监听器端口影响结果时，明确说明。

## Markdown 和 VitePress 规则

EMQX 文档使用 Markdown 编写，并通过 VitePress 渲染。除非需要 EMQX 或 VitePress 扩展语法，否则优先使用标准 Markdown。

### 链接

使用能说明目标内容的链接文字。

推荐：

```markdown
更多信息，参见[配置文件](./configuration.md)。
```

不推荐：

```markdown
更多信息，点击[这里](./configuration.md)。
```

内部文档页面和资源使用相对链接。修改标题后，必须检查锚点是否仍然有效。

### 代码块

代码块使用三个反引号，不使用缩进式代码块。

尽量添加语言标识：

- `bash`：Shell 命令
- `hocon`：EMQX 配置
- `json`：JSON
- `sql`：SQL
- `yaml`：YAML
- `text`：纯文本输出

仅在有助于突出变更或重点时，使用 VitePress 行高亮。

### 自定义容器

需要特别提示的信息可以使用 VitePress 自定义容器。

```markdown
::: tip
用于补充有帮助但非必需的信息。
:::

::: warning
用于说明风险、限制、兼容性问题，或可能导致意外行为的操作。
:::

::: danger
仅用于可能导致数据丢失、安全暴露或服务中断的操作。
:::

::: details
用于折叠可选示例或较长参考内容。
:::
```

不要滥用容器。如果页面大部分内容都在提示或警告中，应重构页面。

### Tabs

当用户需要在多个等价路径中选择一种时，可以使用 tabs，例如不同操作系统、版本形态或工具。

示例：

```markdown
:::: tabs type:card

::: tab EMQX Enterprise
企业版内容。
:::

::: tab EMQX Open Source
开源版内容。
:::

::::
```

Tab 标签应简短，并与 UI 或产品名称保持一致。

### 图片和截图

仅在截图能帮助用户理解 UI 布局、完成复杂操作或识别新增/变更界面时使用截图。

以下内容不建议使用截图：

- 代码示例
- 简单确认对话框
- 许可证文本
- 可以直接写出的错误信息
- 经常变化的页面

截图规则：

- 文件名使用唯一且有意义的英文名称，不包含空格。
- 尽量将截图放在页面对应的 `assets` 目录中。
- 使用相对路径，例如 `![Create Connector](./assets/create-connector.png)`。
- 遮盖或删除主机名、用户名、密码、Token、IP 地址和其他敏感信息。
- 截图聚焦相关 UI 区域。
- 确保截图与当前 UI 标签和顺序一致。
- 不要把必要信息只放在截图中。

### 条件内容

当内容仅适用于 EMQX Open Source 或 EMQX Enterprise 时，使用条件内容块。

```markdown
{% emqxce %}
EMQX Open Source 内容。
{% endemqxce %}

{% emqxee %}
EMQX Enterprise 内容。
{% endemqxee %}
```

如果内容超过一个短语，开始和结束标签应单独成行。

## AI 友好写作

文档应便于用户、搜索引擎和 AI 工具解析。

规则：

- 使用语义明确的标题，避免在同一页面中反复使用“概述”等泛化标题。
- 每个章节聚焦一个主题或任务。
- 示例应自包含。
- 在代码块前提供必要上下文。
- 不要把重要条件只放在截图中。
- 同一概念使用稳定术语。
- 对结构化信息优先使用列表和表格。
- 准确写出配置路径、API 路径、CLI 命令、指标和错误信息。
- 行为因版本而异时，明确说明版本范围。

## 本地化和翻译规则

中文内容应便于与英文源文档同步维护。

规则：

- 不随意删除英文源文档中的技术细节。
- UI 标签、API 路径、配置键、文件路径和代码示例保持不变。
- 不为了中文顺畅而改写技术含义。
- 保留示例、阈值、默认值、版本号和占位符。
- 避免成语、网络用语、比喻和文化依赖表达。
- 相关中英文页面中的术语保持一致。

翻译英文文档时，不要求逐句对应，但必须保持原有逻辑结构和技术信息。

## 导航和仓库结构

文档导航由仓库配置文件生成，并由 CI 检查。

添加或移动页面时：

- 更新目标分支所需的导航配置。
- 使用唯一路径。
- 不要在导航路径中使用锚点。
- 文件位置应符合信息架构。
- 如果目标分支提供检查脚本，应运行对应检查。

不同分支的导航生成流程可能不同。编辑前应检查当前分支中的 `README.md`、`dir.yaml`、`directory.json`、`nav.yaml`、`gen.py` 和相关 CI 脚本。

## API 和配置参考更新

部分 API 和配置参考文件由 EMQX 源码或 Schema 文件生成。

更新生成类参考内容时：

- 先确认事实来源。
- 除非当前分支流程要求手动后处理，否则不要手动重写生成文件。
- 保留生成字段名、枚举值、默认值和说明，除非源内容错误。
- 变更同时适用于开源版和企业版时，应同时更新两者。
- 变更需要本地化时，应同时更新中英文内容。
- 条件允许时，预览生成后的 API 文档。

## 评审清单

提交文档变更前，检查以下项目：

- 页面有且只有一个清晰的一级标题。
- 标题层级正确。
- 页面目的和目标读者清楚。
- 内容类型明确：概念、任务、参考、故障排查或发布说明。
- 技术细节与事实来源一致。
- 与版本相关的行为已明确限定范围。
- EMQX 产品名、功能名、UI 标签、配置路径、API 路径和 CLI 命令准确。
- HOCON 示例使用 `hocon` 代码块，并使用有效配置路径。
- 操作步骤使用祈使句，并包含必要的最后一步和验证步骤。
- 链接文字清晰且链接有效。
- 截图与当前 UI 一致，范围聚焦且不包含敏感信息。
- 表格结构并列，必要时包含单位和默认值。
- 内容简洁、一致，便于中英文同步维护。
- 重要上下文已写入正文，而不是只出现在图片中。
- 没有修改无关文件或无关版本。

## 参考规范

本指南结合 EMQX 文档实践，并参考以下规范和资料：

- [国家标准全文公开系统](https://openstd.samr.gov.cn/bzgk/gb/)
- `GB/T 15834-2011《标点符号用法》`
- `GB/T 15835-2011《出版物上数字用法》`
- `GB/T 3100-1993《国际单位制及其应用》`
- `GB/T 3101-1993《有关量、单位和符号的一般原则》`
- `GB/T 7714-2015《信息与文献 参考文献著录规则》`
- [W3C 中文排版需求](https://www.w3.org/TR/clreq/)
- [VitePress Markdown Extensions](https://vitepress.dev/guide/markdown)
- [Google Developer Documentation Style Guide](https://developers.google.com/style)
- [Microsoft Writing Style Guide](https://learn.microsoft.com/zh-cn/style-guide/welcome/)
- [OASIS DITA 1.3 Specification](https://docs.oasis-open.org/dita/dita/v1.3/dita-v1.3-part1-base.html)
